<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guet.courseselecthelper.mapper.UserMapper">

    <select id="findByAccount" resultType="java.lang.String">
        select password from user where student_id =#{account}
    </select>
</mapper>